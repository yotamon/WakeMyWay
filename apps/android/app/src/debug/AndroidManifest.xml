<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Optional founder Realtime enrichment only. Critical alarm delivery remains offline. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <application android:networkSecurityConfig="@xml/debug_network_security_config">
        <activity
            android:name=".voice.VoiceSpikeActivity"
            android:exported="false"
            android:label="Conversational Alfred" />
    </application>
</manifest>
